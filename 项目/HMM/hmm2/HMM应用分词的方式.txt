使用HMM来做一个分词的操作
-1. 认为文本就是观测值
-2. 基于词语的特性，可以把文字所对应的隐状态认为具有四种可能：
  -a. 文字作为词语开头的那个字，这个称为B隐状态
  -b. 文字作为词语的中间字符，这个称为M隐状态
  -c. 文字作为词语的结尾的字符，这个称为E隐状态
  -d. 文字单独存在组成一个词语，这个称为S隐状态

文字作为词语开头的那个字 --> 文字 作为 词语 开头 的 那个 字
训练过程：
  基于一个已经分好词的训练数据，找出隐状态和观测值文字之间的转换模型参数，然后基于模型参数使用viterbi算法做一个隐状态的预测，最后基于预测的隐状态做一个分词